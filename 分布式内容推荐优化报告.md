# 分布式内容推荐优化报告

> **项目**：校园二手交易平台 · 推荐系统  
> **版本**：Enhanced v2.0  
> **日期**：2026-05-19

---

## 一、优化概述

### 1.1 原始方案的问题

原始 `getSimilarProducts` 仅使用 **单一维度（同分类 + 浏览量排序）** 进行推荐：

```java
// 原始算法：仅按浏览量排序
result.sort((a, b) -> b.getViewCount() - a.getViewCount());
return result.subList(0, limit);
```

**主要缺陷：**

| 问题 | 影响 |
|------|------|
| 仅依赖分类 | 同分类下所有商品"一视同仁"，无法区分"更相似"的商品 |
| 无价格约束 | 50元的书和200元的书会被同等推荐，价格不敏感 |
| 无关键词匹配 | "Java编程"和"计算机网络"同属计算机类但内容不同，仍会互相推荐 |
| 无时效性 | 3年前发布的旧商品与刚发布的新商品推荐优先级相同 |
| 无缓存 | 每次请求都重新查库、排序，高并发下压力大 |

### 1.2 优化目标

- **推荐精准度**：引入多因子评分，使推荐结果更相关
- **响应性能**：引入Redis缓存，TP99 < 50ms
- **可解释性**：展示评分明细，让用户知道"为什么推荐这个"
- **扩展性**：支持水平扩展，多实例共享缓存

---

## 二、多因子相似度算法设计

### 2.1 评分公式

```
综合评分 = 0.40 × 分类匹配 + 0.25 × 价格相似 + 0.20 × 关键词相似 
          + 0.10 × 热度归一 + 0.05 × 时效归一
```

### 2.2 各因子详解

#### ① 分类匹配度（权重 0.40）

```
categoryScore = isSameCategory(current, candidate) ? 1.0 : 0.0
```

同分类是推荐的基础前提。所有候选商品均为同分类商品，此项恒为 1.0，保证推荐结果与当前商品属于同一领域。

#### ② 价格相似度（权重 0.25）

```
priceSimilarity = 1.0 - |price1 - price2| / max(price1, price2)
```

**特性**：
- 价格完全相同时得分为 1.0
- 价格差越大，得分越低（但不会低于 0）
- 例如：¥25 与 ¥30 的相似度为 0.833；与 ¥100 的相似度为 0.25

**降权机制**：
```
if (priceSimilarity < 0.3)  totalScore *= 0.6
```
价格差距超过 2 倍以上的商品，综合评分打六折。

#### ③ 关键词相似度（权重 0.20）

使用 **Jaccard 系数** 计算标题关键词的相似度：

```
keywordSimilarity = |keywords1 ∩ keywords2| / |keywords1 ∪ keywords2|
```

分词使用 `ProductFeatureService` 的自定义词典（支持中英文混合分词）。

**示例**：
```
商品A: "深入理解Java虚拟机" → [深入, 理解, Java, 虚拟机]
商品B: "Java并发编程实战"   → [Java, 并发, 编程, 实战]
交集: {Java} → 1
并集: {深入, 理解, Java, 虚拟机, 并发, 编程, 实战} → 7
Jaccard = 1/7 ≈ 0.143
```

#### ④ 热度归一化（权重 0.10）

```
popularityNorm = min(viewCount / 1000.0, 1.0)
```

浏览量高的商品获得额外加分，但上限为 1.0（浏览量达到 1000 后不再增加热度权重）。

#### ⑤ 时效性（权重 0.05）

```
recencyNorm = 1.0 - min(daysSinceCreation / 365.0, 1.0)
```

发布越近的商品得分越高。365 天后的商品此分降为 0。

### 2.3 约束条件

| 约束 | 说明 | 目的 |
|------|------|------|
| 状态过滤 | `status != 0` 的商品排除 | 不推荐已售/下架商品 |
| 自身排除 | `id != currentProductId` | 不推荐当前商品本身 |
| 最低阈值 | `totalScore < 0.15` 不推荐 | 过滤低质量推荐 |
| 价格降权 | `priceSimilarity < 0.3` 总分 × 0.6 | 价格差距过大的降权 |

---

## 三、分布式缓存策略

### 3.1 架构设计

```
┌──────────┐    ┌──────────┐    ┌──────────┐
│ Tomcat 1 │    │ Tomcat 2 │    │ Tomcat N │  ← 水平扩展
├──────────┤    ├──────────┤    ├──────────┤
│ 本地缓存 │    │ 本地缓存 │    │ 本地缓存 │  ← L1: 可选（Caffeine）
└─────┬────┘    └─────┬────┘    └─────┬────┘
      └───────────────┼───────────────┘
                      │
              ┌───────▼───────┐
              │    Redis      │  ← L2: 分布式缓存
              │  Cluster      │
              └───────┬───────┘
                      │
              ┌───────▼───────┐
              │    MySQL      │  ← L3: 数据源
              └───────────────┘
```

### 3.2 Redis Key 设计

```
Key: "similar:enhanced:{productId}"  (Hash)
├── field: "products"  → 推荐商品列表 (JSON)
├── field: "details"   → 评分明细列表 (JSON)
└── field: "cachedAt"  → 缓存时间戳

TTL: 3600 秒（1小时）
```

### 3.3 缓存一致性保障

| 场景 | 策略 |
|------|------|
| **缓存未命中** | 计算并回填，加分布式锁防止缓存击穿 |
| **缓存过期** | 被动过期，下次请求重新计算 |
| **商品更新** | 调用 `clearSimilarCache(productId)` 主动清除 |
| **并发回填** | 使用 Redis 分布式锁互斥，等待线程回退读缓存 |

### 3.4 缓存击穿防护

```java
// 伪代码：互斥锁 + 双重检查
if (cache.has(key)) return cache.get(key);
if (lock.acquire(key)) {
    if (cache.has(key)) return cache.get(key); // 双重检查
    result = computeSimilarity();
    cache.put(key, result);
    lock.release(key);
    return result;
} else {
    Thread.sleep(50);
    return cache.get(key); // 等待后读缓存
}
```

---

## 四、性能对比分析

### 4.1 时间复杂度

| 阶段 | 原始算法 | 增强算法 | 说明 |
|------|---------|---------|------|
| 数据库查询 | O(1) | O(1) | 均需查询商品和同分类列表 |
| 排序 | O(n log n) | O(n log n) | 增强版多因子排序常数更大 |
| 缓存读取 | ❌ 无缓存 | O(1) | 缓存命中时近乎零耗时 |
| 关键词计算 | ❌ 未涉及 | O(k × n) | k为关键词数，n为候选数 |

> **缓存命中时**：增强版性能远优于原始版（Redis读取 vs MySQL查询+排序）  
> **缓存未命中时**：增强版多 100~300ms（关键词提取+多因子计算）

### 4.2 推荐精准度对比

| 维度 | 原始算法 | 增强算法 |
|------|---------|---------|
| 分类匹配 | ✅ 完全匹配 | ✅ 完全匹配 |
| 价格敏感 | ❌ 不考虑 | ✅ 价差越小分越高 |
| 关键词匹配 | ❌ 不考虑 | ✅ Jaccard系数 |
| 热度加分 | ✅ 按浏览量排序 | ✅ 归一化加分 |
| 时效加分 | ❌ 不考虑 | ✅ 新商品加分 |
| 低质过滤 | ❌ 不过滤 | ✅ 阈值过滤+降权 |

### 4.3 分布式场景优势

| 场景 | 原始算法（无缓存） | 增强算法（Redis缓存） |
|------|------------------|-------------------|
| 单实例 100 QPS | 100次DB查询 | 1次计算 + 99次缓存读取 |
| 双实例 200 QPS | 200次DB查询 | 1次计算 + 199次缓存读取 |
| N实例 N×100 QPS | N×100次DB查询 | 1次计算 + (N×100-1)次缓存读取 |
| 数据库压力 | ❌ 线性增长 | ✅ 几乎恒定 |

---

## 五、部署与配置

### 5.1 新增/修改文件清单

| 文件 | 操作 | 说明 |
|------|------|------|
| `EnhancedRecommendService.java` | 新增 | 增强版推荐服务接口 |
| `EnhancedRecommendServiceImpl.java` | 新增 | 多因子评分 + Redis缓存实现 |
| `ProductController.java` | 修改 | 集成增强推荐，降级到原始推荐 |
| `detail.jsp` | 修改 | 显示评分进度条 |

### 5.2 依赖关系

```
ProductController
    ├── EnhancedRecommendService ← 新增
    │       ├── ProductMapper
    │       ├── ProductFeatureService（分词）
    │       └── RedisTemplate（缓存）
    │
    └── RecommendService（降级兜底）
            └── 原始 getSimilarProducts
```

### 5.3 降级链路

```
正常流程: EnhancedRecommendService (Redis缓存命中)
    → 返回缓存结果（< 5ms）

缓存未命中: EnhancedRecommendService (计算+回填)
    → MySQL查询 → 多因子评分 → Redis写入
    → 返回结果（100~300ms）

服务异常: RecommendService (降级)
    → 原始同分类+浏览量排序
    → 返回结果（< 50ms）
```

---

## 六、未来优化方向

### 6.1 短期（v2.1）

- **L1本地缓存**：引入 Caffeine 本地缓存，减少 Redis 网络开销
- **异步预热**：商品发布后异步预计算相似推荐
- **评分调参**：通过 A/B 测试优化各因子权重

### 6.2 中期（v2.2）

- **协同过滤补充**：引入"看了又看"（Frequently Bought Together）
- **实时特征更新**：用户行为实时影响商品特征权重
- **批量预计算**：定时任务离线计算所有商品的相似推荐

### 6.3 长期（v3.0）

- **向量化召回**：使用 Embedding 将商品映射到向量空间
- **ANN 索引**：使用 Faiss / HNSW 进行近似最近邻搜索
- **多级排序**：粗排（多因子）→ 精排（DNN CTR 预估）

---

## 七、测试验收方法

### 7.1 功能验收

```bash
# 1. 访问商品详情页，查看底部"相似商品推荐"
curl http://localhost:8080/product/detail?id=5

# 2. 验证Redis缓存
redis-cli KEYS "similar:enhanced:*"

# 3. 验证缓存TTL
redis-cli TTL "similar:enhanced:5"

# 4. 验证缓存一致性（更新商品后）
curl -X POST "http://localhost:8080/product/update?id=5&name=新名称"
redis-cli KEYS "similar:enhanced:5"   # 应已被清除
```

### 7.2 性能验收

```bash
# 使用 AB/Apache Bench 进行压测
ab -n 1000 -c 10 "http://localhost:8080/product/detail?id=5"

# 观察指标
# 首次请求：100~300ms（缓存未命中）
# 后续请求：< 5ms（缓存命中）
```

### 7.3 分布式验收

```bash
# 在 Tomcat1 上触发计算
curl "http://localhost:8080/product/detail?id=5"

# 在 Tomcat2 上验证缓存共享
curl "http://localhost:8081/product/detail?id=5"
# 应无需重复计算，直接从缓存读取
```

---

> **报告总结**：增强版推荐算法通过**多因子评分**将推荐精准度提升了 3 个维度（价格、关键词、时效），通过 **Redis 分布式缓存**使高并发场景下的数据库压力降低 99%+，并设计了完整的**降级链路**保障系统可用性。
